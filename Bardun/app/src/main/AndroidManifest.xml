<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="no.hiof.oleedvao.bardun">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">

        </activity>
        <activity android:name=".BrukerActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TeltplassActivity" />
        <activity android:name=".RedigerBrukerActivity" />
        <activity android:name=".RedigerTeltplassActivity" />
        <activity android:name=".InstillingerActivity" />
        <activity android:name=".OpprettKommentarActivity" />
        <activity android:name=".OpprettTeltplassActivity"></activity>
    </application>

</manifest>